<%= form_with(model: academic_project, local: true) do |form| %>
  <% if academic_project.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(academic_project.errors.count, "error") %> prohibited this academic_project from being saved:</h2>

      <ul>
      <% academic_project.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :title %>
    <%= form.text_field :title %>
  </div>

  <div class="field">
    <%= form.label :description %>
    <%= form.text_area :description %>
  </div>

  <div class="field">
    <%= form.label :role %>
    <%= form.text_field :role %>
  </div>

  <div class="field">
    <%= form.label :student_id %>
    <%= form.text_field :student_id %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
